/** Shared tariff UI / lookup constants — safe to import from Client Components (no Mongoose). */

/** Zone code for a single tariff row that applies to all geographic zones in the ordinance */
export const ALL_ZONES_TARIFF_CODE = 'all';
export const ALL_ZONES_LABEL_HE = 'כל האזורים';
